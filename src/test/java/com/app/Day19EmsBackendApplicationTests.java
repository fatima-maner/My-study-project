package com.app;


import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;



@SpringBootTest
class Day19EmsBackendApplicationTests {

	
	  @Test void contextLoads() { }
		
		/*
		 * @Test public void testFindMax(){ int result=TestLogic.calculate(2,6); int
		 * Expected=8; Assert.assertEquals(Expected,result);
		 * 
		 * }
		 */
		/*
		 * @Test public void testGetPatientById() {
		 * 
		 * // Mock or prepare necessary data PatientServiceImpl patientService = new
		 * PatientServiceImpl();
		 * 
		 * // Invoke the method being tested Patient patient =
		 * patientService.getPatientDetails(1);
		 * 
		 * // Assert the result Assert.assertNotNull(patient); Object result=1;
		 * //Assert.assertEquals(123, patient.getId()); // Assert.assertEquals(result,
		 * patient.getId()); Assert.assertEquals(result, patient.getId()); // Add more
		 * assertions as needed }
		 */
		  
}
